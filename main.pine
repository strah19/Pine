/*
a : int = 1 + 2 + 2;
b : int = 2;
c : int = 4;

a = b = c;

print a + b + c;
print "a";
*/

//This would be the same as typedef in C
//<define> boolean : int;

/*
<define> boolean : int;
<define> boolean : int = (true : false);

<import> module;
<run> method;

<if> comparison
<end> 
*/

range(start: int, end: const int) {
	if start == end {
		return;
	}

	print start;
	start = start + 1;
	range(start, end);
}

lrange(start: int, end: const int) {
	while start <= end {
		print start;
		start = start + 1;
	}
}

add(num1: int, num2: int) -> int {
	return num1 + num2;
}

//Comment

range(0, 10);

`